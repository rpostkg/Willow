<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Willow.Views.PrivacyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock x:Uid="PrivacyPage_Title"
                   Grid.Row="0"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="0,0,0,20"/>

        <!-- Score card -->
        <Border Grid.Row="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16,12"
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="6" VerticalAlignment="Center">
                    <TextBlock x:Uid="PrivacyPage_ScoreTitle"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <Border Background="{x:Bind ViewModel.ScoreBackground, Mode=OneWay}"
                            CornerRadius="4"
                            Padding="8,3"
                            HorizontalAlignment="Left">
                        <TextBlock Text="{x:Bind ViewModel.ScoreLabel, Mode=OneWay}"
                                   FontWeight="SemiBold"
                                   FontSize="13"
                                   Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                    </Border>
                </StackPanel>

                <Button Grid.Column="1"
                        x:Uid="PrivacyPage_ViewTweaksButton"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="ViewTweaksButton_Click"
                        VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Capability access header -->
        <TextBlock x:Uid="PrivacyPage_CapabilityHeader"
                   Grid.Row="2"
                   Style="{StaticResource SubtitleTextBlockStyle}"
                   Opacity="0.6"
                   Margin="0,0,0,12"/>

        <!-- Global capability toggles -->
        <StackPanel Grid.Row="3" Spacing="8">

            <InfoBar x:Uid="PrivacyPage_PolicyInfoBar"
                     IsOpen="True"
                     IsClosable="False"
                     Severity="Informational"/>

            <!-- Camera -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16,12">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Grid.Column="0"
                              Glyph="&#xE722;"
                              FontSize="20"
                              VerticalAlignment="Center"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                        <TextBlock x:Uid="PrivacyPage_CameraTitle" FontWeight="SemiBold"/>
                        <TextBlock x:Uid="PrivacyPage_CameraDesc"
                                   FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    <ToggleSwitch Grid.Column="2"
                                  IsOn="{x:Bind ViewModel.CameraEnabled, Mode=TwoWay}"
                                  OnContent=""
                                  OffContent=""
                                  VerticalAlignment="Center"
                                  Margin="0"/>
                </Grid>
            </Border>

            <!-- Microphone -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16,12">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Grid.Column="0"
                              Glyph="&#xE720;"
                              FontSize="20"
                              VerticalAlignment="Center"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                        <TextBlock x:Uid="PrivacyPage_MicTitle" FontWeight="SemiBold"/>
                        <TextBlock x:Uid="PrivacyPage_MicDesc"
                                   FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    <ToggleSwitch Grid.Column="2"
                                  IsOn="{x:Bind ViewModel.MicEnabled, Mode=TwoWay}"
                                  OnContent=""
                                  OffContent=""
                                  VerticalAlignment="Center"
                                  Margin="0"/>
                </Grid>
            </Border>

            <!-- Location -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16,12">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Grid.Column="0"
                              Glyph="&#xE81D;"
                              FontSize="20"
                              VerticalAlignment="Center"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                        <TextBlock x:Uid="PrivacyPage_LocationTitle" FontWeight="SemiBold"/>
                        <TextBlock x:Uid="PrivacyPage_LocationDesc"
                                   FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    <ToggleSwitch Grid.Column="2"
                                  IsOn="{x:Bind ViewModel.LocationEnabled, Mode=TwoWay}"
                                  OnContent=""
                                  OffContent=""
                                  VerticalAlignment="Center"
                                  Margin="0"/>
                </Grid>
            </Border>

        </StackPanel>
    </Grid>
</Page>
